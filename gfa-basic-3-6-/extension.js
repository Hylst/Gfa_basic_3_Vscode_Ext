const vscode = require('vscode');

function activate(context) {
  console.log('GFA BASIC extension is now active!');
}

function deactivate() {}

module.exports = {
  activate,
  deactivate
};